import { ValidateTaskExpiryDirective } from './validate-task-expiry.directive';

describe('ValidateTaskExpiryDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateTaskExpiryDirective();
    expect(directive).toBeTruthy();
  });
});
